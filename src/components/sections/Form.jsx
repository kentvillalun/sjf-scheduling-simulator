import { useState } from "react"


export const Form = () => {

    const [step, setStep] = useState(1);

    return (
        <section className="flex flex-col justify-center items-center m-auto text-center p-6 gap-2">
            {}
        </section>
    )
}